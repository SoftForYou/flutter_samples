import 'package:flutter/material.dart';

class LabNetworkScreen extends StatelessWidget {
  const LabNetworkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('LabNetWork Page'));
  }
}
